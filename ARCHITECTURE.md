# User Management System - Architecture & Flow

## 🏗️ System Architecture

### Component Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    WEB BROWSER                                  │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  User List   │  │  Add User    │  │  Edit User   │          │
│  │  (users.php) │  │ (users_add)  │  │ (users_edit) │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
          │                  │                  │
          └──────────────────┼──────────────────┘
                            │
                    ┌───────▼────────┐
                    │  Admin Panel   │
                    │  Navigation    │
                    │ (header.php)   │
                    └────────────────┘
                            │
   ┌────────────────────────┼────────────────────────┐
   │                        │                        │
   ▼                        ▼                        ▼
┌──────────────┐   ┌──────────────┐   ┌──────────────┐
│   Session    │   │ User         │   │  Database    │
│ Management   │   │ Functions    │   │  Config      │
│(session.php) │   │(users_fn.php)│   │(db_config)   │
└──────────────┘   └──────────────┘   └──────────────┘
                            │
                            ▼
                    ┌──────────────────┐
                    │   MySQL Server   │
                    │                  │
                    │  users table     │
                    │  (all user data) │
                    └──────────────────┘
```

---

## 📊 Data Flow Diagram

### Creating a New User

```
┌─────────────────┐
│ Admin clicks    │
│ "Add User"      │
│ button          │
└────────┬────────┘
         │
         ▼
┌─────────────────────────┐
│ Load users_add.php      │
│ Display form with       │
│ - Basic Info fields     │
│ - Role selection        │
│ - Professional info     │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Admin fills form:       │
│ - Username              │
│ - Email                 │
│ - Name                  │
│ - Password (or auto)    │
│ - Select Role           │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Form submitted (POST)   │
│ Validate input:         │
│ - Check duplicates      │
│ - Validate format       │
│ - Hash password         │
└────────┬────────────────┘
         │
     ┌───┴───┐
     │       │
     ▼       ▼
   ✓OK     ✗Error
   │         │
   │         ▼
   │      ┌─────────────┐
   │      │ Show errors │
   │      │ on form     │
   │      └─────────────┘
   │
   ▼
┌──────────────────────────┐
│ Insert into users table: │
│ INSERT INTO users (...)  │
│ VALUES (...)             │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ User created             │
│ Redirect to users.php    │
│ with success message     │
└──────────────────────────┘
```

### Editing an Existing User

```
┌──────────────────────┐
│ Admin clicks         │
│ "Edit" button        │
│ on user row          │
└────────┬─────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Load users_edit.php?id=X     │
│ - Get user data from DB      │
│ - Populate form fields       │
│ - Show current values        │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Admin updates fields:        │
│ - Can change name            │
│ - Can change role            │
│ - Can change status          │
│ - Can change password        │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Form submitted (POST)        │
│ Validate changes:            │
│ - Check for duplicates       │
│ - Validate format            │
│ - Hash new password (if any) │
└────────┬─────────────────────┘
         │
     ┌───┴───┐
     │       │
     ▼       ▼
   ✓OK     ✗Error
   │         │
   │         ▼
   │      ┌─────────────┐
   │      │ Show errors │
   │      └─────────────┘
   │
   ▼
┌──────────────────────────┐
│ Update in database:      │
│ UPDATE users SET ...     │
│ WHERE id = X             │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ User updated             │
│ Redirect to users.php    │
│ with success message     │
└──────────────────────────┘
```

### Viewing Users List

```
┌──────────────────────┐
│ Admin clicks         │
│ "All Users"          │
│ in navigation        │
└────────┬─────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Load users.php               │
│ Get query parameters:        │
│ - search term?               │
│ - role filter?               │
│ - status filter?             │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Build SQL query:             │
│ SELECT * FROM users          │
│ WHERE conditions             │
│ ORDER BY created_at          │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Execute query                │
│ Get result set               │
│ Calculate statistics         │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Render page:                 │
│ - Display stats cards        │
│ - Show filters form          │
│ - Display users table        │
│ - Show action buttons        │
└──────────────────────────────┘
```

---

## 🗂️ File Structure & Relationships

```
admin/
│
├── users.php
│   ├── includes: session.php (authentication)
│   ├── includes: users_functions.php (getAllUsers, deleteUser)
│   └── displays: User list with filters
│
├── users_add.php
│   ├── includes: session.php (authentication)
│   ├── includes: users_functions.php (addUser, validateUserInput)
│   └── displays: Add user form
│
├── users_edit.php
│   ├── includes: session.php (authentication)
│   ├── includes: users_functions.php (getUserById, updateUser, updateUserPassword)
│   └── displays: Edit user form
│
├── users_functions.php
│   ├── includes: db_config.php (database connection)
│   └── contains: 12 helper functions
│
├── verify_users.php
│   ├── includes: db_config.php (database connection)
│   └── displays: System verification checklist
│
├── header.php (MODIFIED)
│   ├── includes navigation menu
│   ├── added: Users dropdown with links to users.php and users_add.php
│   └── displays: Navigation bar with user menu
│
├── footer.php
│   └── displays: Page footer with scripts
│
├── session.php
│   └── verifies: Admin authentication before page load
│
└── dashboard.php
    └── displays: Admin dashboard

db_config.php
├── defines: Database connection constants
├── creates: Connection to MySQL
└── contains: Helper functions like getAllRows, deleteData

database.sql (MODIFIED)
├── contains: CREATE TABLE statements
├── includes: users table definition (NEW!)
└── includes: INSERT statements for default users (NEW!)

assets/
├── css/
│   ├── style.css
│   │   └── contains: All main styling (includes user management styles)
│   └── responsive.css
│       └── contains: Responsive design for all screen sizes
│
└── js/
    └── script.js
        └── contains: Common JavaScript functionality
```

---

## 🔐 Security Flow

### Authentication & Authorization

```
┌──────────────┐
│ User Request │
└──────┬───────┘
       │
       ▼
┌──────────────────────────┐
│ session.php              │
│ Check: $_SESSION set?    │
└────────┬─────────────────┘
         │
     ┌───┴───┐
     │       │
  YES│      NO│
     │       │
     ▼       ▼
   ✓Pass  ╔═══════════════╗
     │    ║ Redirect to   ║
     │    ║ login page    ║
     │    ║ Exit script   ║
     │    ╚═══════════════╝
     │
     ▼
┌──────────────┐
│ Load page    │
│ Execute PHP  │
└──────────────┘
```

### Password Hashing & Validation

```
                    Password Input
                          │
                ┌─────────┴─────────┐
                │                   │
         Add User              Edit User
           (New)             (Change Pass)
             │                     │
             ▼                     ▼
    ┌─────────────────┐   ┌──────────────┐
    │ password_hash() │   │ password_hash()
    │ with DEFAULT    │   │ with DEFAULT
    └─────────────────┘   └──────────────┘
             │                     │
             └─────────┬───────────┘
                       │
                       ▼
            ┌────────────────────┐
            │ Store in database  │
            │ (encrypted)        │
            └────────────────────┘
```

### Input Validation

```
User Input
    │
    ▼
┌──────────────────────┐
│ Trim whitespace      │
└────────┬─────────────┘
         │
         ▼
┌──────────────────────┐
│ Check if empty       │
└────────┬─────────────┘
         │
         ▼
┌──────────────────────┐
│ Validate format      │
│ (email, username)    │
└────────┬─────────────┘
         │
         ▼
┌──────────────────────┐
│ Check duplicates     │
│ (uniqueness)         │
└────────┬─────────────┘
         │
         ▼
┌──────────────────────┐
│ Sanitize output      │
│ htmlspecialchars()   │
└────────┬─────────────┘
         │
     ┌───┴───┐
     │       │
  ✓OK    ✗Error
     │       │
     ▼       ▼
  Process  Display
           Error
```

---

## 📱 Responsive Design Breakpoints

```
┌─────────────────────────────────────────────────────────┐
│ Desktop (1200px+)                                       │
│ ┌───────────────────────────────────────────────────┐   │
│ │ Stat Card | Stat Card | Stat Card | Stat Card   │   │
│ ├───────────────────────────────────────────────────┤   │
│ │ Filter Input | Role Select | Status Select | [🔍] │  │
│ ├───────────────────────────────────────────────────┤   │
│ │ Username | Email | Name | Role | Status | Actions│   │
│ │ ---------|-------|------|------|--------|---------|   │
│ │ user1    | em1   | name | role | status | edit   │   │
│ ├───────────────────────────────────────────────────┤   │
│ └───────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘

┌──────────────────────────────────┐
│ Tablet (768px - 1199px)          │
│ ┌──────────────┬──────────────┐   │
│ │ Stat Card    │ Stat Card    │   │
│ ├──────────────┼──────────────┤   │
│ │ Stat Card    │ Stat Card    │   │
│ ├──────────────────────────────┤   │
│ │ Filter Input                 │   │
│ │ Role Select | Status Select  │   │
│ ├──────────────────────────────┤   │
│ │ Username | Email | Actions   │   │
│ ├──────────────────────────────┤   │
│ │ user1    | em1   | edit delt │   │
│ └──────────────────────────────┘   │
└──────────────────────────────────┘

┌──────────────────────┐
│ Mobile (<768px)      │
│ ┌──────────────────┐  │
│ │ Stat: Total = 5  │  │
│ ├──────────────────┤  │
│ │ Stat: Students=3 │  │
│ ├──────────────────┤  │
│ │ Filter Input     │  │
│ ├──────────────────┤  │
│ │ User: user1      │  │
│ │ Email: em1       │  │
│ │ [Edit] [Delete]  │  │
│ └──────────────────┘  │
└──────────────────────┘
```

---

## 🔄 Database Transaction Flow

### Adding User Transaction

```
BEGIN TRANSACTION
     │
     ▼
┌──────────────────────────┐
│ Check username exists    │
│ SELECT * FROM users      │
│ WHERE username = 'X'     │
└────────┬─────────────────┘
         │
     ┌───┴─────────┐
     │             │
   Unique      Duplicate
     │             │
     │             ▼
     │         ROLLBACK
     │         (Error)
     │
     ▼
┌──────────────────────────┐
│ Check email exists       │
│ SELECT * FROM users      │
│ WHERE email = 'X@Y'      │
└────────┬─────────────────┘
         │
     ┌───┴─────────┐
     │             │
   Unique      Duplicate
     │             │
     │             ▼
     │         ROLLBACK
     │         (Error)
     │
     ▼
┌──────────────────────────┐
│ Insert new user          │
│ INSERT INTO users (...)  │
│ VALUES (...)             │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ COMMIT TRANSACTION       │
│ Changes are permanent    │
└──────────────────────────┘
```

---

## 🎯 User Role Hierarchy

```
                    ┌─────────────┐
                    │ System Root │
                    └──────┬──────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
    ┌───────────┐   ┌──────────────┐  ┌──────────────┐
    │Administrator   │   Teacher   │  │   Student    │
    │              │              │  │              │
    │ • Full access│ • Class mgmt │  │ • View grades│
    │ • User mgmt  │ • Mark       │  │ • Assignments│
    │ • Settings   │   attendance │  │ • Exam results
    │ • Reports    │ • Set marks  │  │ • Fee details│
    └───────────┘   └──────────────┘  └──────────────┘
        │                  │
        └────────┬─────────┘
                 │
                 ▼
        ┌──────────────────┐
        │ Non-Teaching Staff
        │                  │
        │ • Admin support  │
        │ • Facility mgmt  │
        │ • Report access  │
        └──────────────────┘
```

---

## 📊 Database Relationships

```
┌─────────────────────────────────────────────────┐
│ users TABLE (Central)                           │
├─────────────────────────────────────────────────┤
│ id (PK)           │ PRIMARY KEY                 │
│ username (UNIQUE) │ For login/identification    │
│ password          │ Hashed password             │
│ email (UNIQUE)    │ For contact/notifications   │
│ role              │ Foreign concept (not FK)*   │
│ status            │ active / inactive           │
│ first_name        │ Display name                │
│ last_name         │ Display name                │
│ ... other fields  │                             │
└─────────────────────────────────────────────────┘
         │
         │ (Can be extended with ForeignKeys)
         │
    ┌────┴────┐
    │ Roles    │ (Possible future table)
    │ Permissions│ (Possible future table)
    └──────────┘
```

---

## ✨ Key Features Summary

```
User Management System
│
├── User CRUD Operations
│   ├── Create (Add User)
│   ├── Read (View Users)
│   ├── Update (Edit User)
│   └── Delete (Remove User)
│
├── Search & Filter
│   ├── Search by name/email
│   ├── Filter by role
│   └── Filter by status
│
├── Role Management
│   ├── Student role
│   ├── Teacher role
│   ├── Non-Teaching Staff role
│   └── Administrator role
│
├── Security
│   ├── Password hashing
│   ├── Input validation
│   ├── Duplicate checking
│   └── Session authentication
│
├── User Interface
│   ├── Statistics dashboard
│   ├── Responsive design
│   ├── Intuitive navigation
│   └── Clear error messages
│
└── Documentation
    ├── User guides
    ├── Admin documentation
    ├── API reference
    └── Troubleshooting
```

---

**Architecture Version**: 1.0  
**Last Updated**: 2024  
**System**: School Management System - User Management Module
